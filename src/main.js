var map; 

// ------------------------------- Sketch Setup ------------------------------
function setup() {
  // Remove Canvas
  noCanvas(); 

  // Initialize map using D3. 
  map = new Map(windowWidth, windowHeight); 
}

// ------------------------------- Sketch Draw (loop) ------------------------
function draw() {
}